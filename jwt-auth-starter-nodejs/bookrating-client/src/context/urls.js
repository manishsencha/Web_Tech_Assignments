
const baseUrl = "http://localhost:5000"
const urls = {
    SIGNIN: `${baseUrl}/signin`,
    SIGNUP: `${baseUrl}/signup`,
    BOOKS: `${baseUrl}/books`,
    IMAGE: `${baseUrl}/image`,
    RATE: `${baseUrl}/rate`,
    VERIFY: `${baseUrl}/verify`
}

export default urls